# autogenerated with ./generate_motor_cfg.py /home/embedded/xbot2_ws/src/iit-nexus-ros-pkg/nexus_urdf/urdf/nexus.urdf

zmq_setup:
  timeout: 1000

definitions:
 - &default_imp_gain [100.0, 5.0, 1.0, 0.003, 0.40]
 - &default_pos_gain [100.0, 0.0, 5.0]
 - &ctrl_mode idle



torso_yaw_joint:
  control_mode: *ctrl_mode
  pid:
    position: *default_pos_gain
    impedance: *default_imp_gain



r_shoulder_pitch_joint:
  control_mode: *ctrl_mode
  pid:
    position: *default_pos_gain
    impedance: *default_imp_gain



r_shoulder_roll_joint:
  control_mode: *ctrl_mode
  pid:
    position: *default_pos_gain
    impedance: *default_imp_gain



r_shoulder_yaw_joint:
  control_mode: *ctrl_mode
  pid:
    position: *default_pos_gain
    impedance: *default_imp_gain



r_elbow_joint:
  control_mode: *ctrl_mode
  pid:
    position: *default_pos_gain
    impedance: *default_imp_gain



r_forearm_joint:
  control_mode: *ctrl_mode
  pid:
    position: *default_pos_gain
    impedance: *default_imp_gain



r_wrist_pitch_joint:
  control_mode: *ctrl_mode
  pid:
    position: *default_pos_gain
    impedance: *default_imp_gain



r_wrist_yaw_joint:
  control_mode: *ctrl_mode
  pid:
    position: *default_pos_gain
    impedance: *default_imp_gain



l_shoulder_pitch_joint:
  control_mode: *ctrl_mode
  pid:
    position: *default_pos_gain
    impedance: *default_imp_gain



l_shoulder_roll_joint:
  control_mode: *ctrl_mode
  pid:
    position: *default_pos_gain
    impedance: *default_imp_gain



l_shoulder_yaw_joint:
  control_mode: *ctrl_mode
  pid:
    position: *default_pos_gain
    impedance: *default_imp_gain



l_elbow_joint:
  control_mode: *ctrl_mode
  pid:
    position: *default_pos_gain
    impedance: *default_imp_gain



l_forearm_joint:
  control_mode: *ctrl_mode
  pid:
    position: *default_pos_gain
    impedance: *default_imp_gain



l_wrist_pitch_joint:
  control_mode: *ctrl_mode
  pid:
    position: *default_pos_gain
    impedance: *default_imp_gain



l_wrist_yaw_joint:
  control_mode: *ctrl_mode
  pid:
    position: *default_pos_gain
    impedance: *default_imp_gain



tof_camera_joint:
  control_mode: *ctrl_mode
  pid:
    position: *default_pos_gain
    impedance: *default_imp_gain



lidar_joint:
  control_mode: *ctrl_mode
  pid:
    position: *default_pos_gain
    impedance: *default_imp_gain



fr_hip_yaw_joint:
  control_mode: *ctrl_mode
  pid:
    position: *default_pos_gain
    impedance: *default_imp_gain



fr_hip_pitch_joint:
  control_mode: *ctrl_mode
  pid:
    position: *default_pos_gain
    impedance: *default_imp_gain



fr_knee_joint:
  control_mode: *ctrl_mode
  pid:
    position: *default_pos_gain
    impedance: *default_imp_gain



fr_ankle_pitch_joint:
  control_mode: *ctrl_mode
  pid:
    position: *default_pos_gain
    impedance: *default_imp_gain



fr_ankle_yaw_joint:
  control_mode: *ctrl_mode
  pid:
    position: *default_pos_gain
    impedance: *default_imp_gain



fr_wheel_joint:
  control_mode: *ctrl_mode
  pid:
    position: *default_pos_gain
    impedance: *default_imp_gain



fl_hip_yaw_joint:
  control_mode: *ctrl_mode
  pid:
    position: *default_pos_gain
    impedance: *default_imp_gain



fl_hip_pitch_joint:
  control_mode: *ctrl_mode
  pid:
    position: *default_pos_gain
    impedance: *default_imp_gain



fl_knee_joint:
  control_mode: *ctrl_mode
  pid:
    position: *default_pos_gain
    impedance: *default_imp_gain



fl_ankle_pitch_joint:
  control_mode: *ctrl_mode
  pid:
    position: *default_pos_gain
    impedance: *default_imp_gain



fl_ankle_yaw_joint:
  control_mode: *ctrl_mode
  pid:
    position: *default_pos_gain
    impedance: *default_imp_gain



fl_wheel_joint:
  control_mode: *ctrl_mode
  pid:
    position: *default_pos_gain
    impedance: *default_imp_gain



hr_hip_yaw_joint:
  control_mode: *ctrl_mode
  pid:
    position: *default_pos_gain
    impedance: *default_imp_gain



hr_hip_pitch_joint:
  control_mode: *ctrl_mode
  pid:
    position: *default_pos_gain
    impedance: *default_imp_gain



hr_knee_joint:
  control_mode: *ctrl_mode
  pid:
    position: *default_pos_gain
    impedance: *default_imp_gain



hr_ankle_pitch_joint:
  control_mode: *ctrl_mode
  pid:
    position: *default_pos_gain
    impedance: *default_imp_gain



hr_ankle_yaw_joint:
  control_mode: *ctrl_mode
  pid:
    position: *default_pos_gain
    impedance: *default_imp_gain



hr_wheel_joint:
  control_mode: *ctrl_mode
  pid:
    position: *default_pos_gain
    impedance: *default_imp_gain



hl_hip_yaw_joint:
  control_mode: *ctrl_mode
  pid:
    position: *default_pos_gain
    impedance: *default_imp_gain



hl_hip_pitch_joint:
  control_mode: *ctrl_mode
  pid:
    position: *default_pos_gain
    impedance: *default_imp_gain



hl_knee_joint:
  control_mode: *ctrl_mode
  pid:
    position: *default_pos_gain
    impedance: *default_imp_gain



hl_ankle_pitch_joint:
  control_mode: *ctrl_mode
  pid:
    position: *default_pos_gain
    impedance: *default_imp_gain



hl_ankle_yaw_joint:
  control_mode: *ctrl_mode
  pid:


hl_wheel_joint:
  control_mode: *ctrl_mode
  pid:
    position: *default_pos_gain
    impedance: *default_imp_gain

right_hand:
  control_mode: *ctrl_mode
  pid :
    position: [100000.0 , 0.0 , 1.0]

left_hand:
  control_mode: *ctrl_mode
  pid :
    position: [100000.0 , 0.0 , 1.0]
