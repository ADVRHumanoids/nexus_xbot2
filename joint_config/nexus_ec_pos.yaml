# autogenerated with ./generate_motor_cfg.py /home/embedded/xbot2_ws/src/iit-nexus-ros-pkg/nexus_urdf/urdf/nexus.urdf

zmq_setup:
  timeout: 1000

definitions:
 - &default_imp_gain [100.0, 5.0, 1.0, 0.003, 0.40]
 - &default_pos_gain [100.0, 0.0, 5.0]
 - &pos_gain_arm_mid [500.0, 0.0, 20.0]
 - &pos_gain_arm_sm [200.0, 0.0, 10.0]
 - &pos_gain_leg_big [3000.0, 0.0, 50.0]
 - &pos_gain_leg_mid [1000.0, 0.0, 30.0]
 - &pos_gain_leg_yaw [300.0, 0.0, 10.0]
 - &default_wheel [30.0, 0.0, 0.0]
 - &ctrl_mode_upperb 3B_motor_pos_ctrl
 - &ctrl_mode_lowerb 3B_motor_pos_ctrl


torso_yaw_joint:
  control_mode: *ctrl_mode_upperb
  pid:
    position: *pos_gain_arm_mid
    impedance: *default_imp_gain



r_shoulder_pitch_joint:
  control_mode: *ctrl_mode_upperb
  pid:
    position: *pos_gain_arm_mid
    impedance: *default_imp_gain



r_shoulder_roll_joint:
  control_mode: *ctrl_mode_upperb
  pid:
    position: *pos_gain_arm_mid
    impedance: *default_imp_gain



r_shoulder_yaw_joint:
  control_mode: *ctrl_mode_upperb
  pid:
    position: *pos_gain_arm_mid
    impedance: *default_imp_gain



r_elbow_joint:
  control_mode: *ctrl_mode_upperb
  pid:
    position: *pos_gain_arm_mid
    impedance: *default_imp_gain



r_forearm_joint:
  control_mode: *ctrl_mode_upperb
  pid:
    position: *pos_gain_arm_sm
    impedance: *default_imp_gain



r_wrist_pitch_joint:
  control_mode: *ctrl_mode_upperb
  pid:
    position: *pos_gain_arm_sm
    impedance: *default_imp_gain


l_shoulder_pitch_joint:
  control_mode: *ctrl_mode_upperb
  pid:
    position: *pos_gain_arm_mid
    impedance: *default_imp_gain



l_shoulder_roll_joint:
  control_mode: *ctrl_mode_upperb
  pid:
    position: *pos_gain_arm_mid
    impedance: *default_imp_gain



l_shoulder_yaw_joint:
  control_mode: *ctrl_mode_upperb
  pid:
    position: *pos_gain_arm_mid
    impedance: *default_imp_gain



l_elbow_joint:
  control_mode: *ctrl_mode_upperb
  pid:
    position: *pos_gain_arm_mid
    impedance: *default_imp_gain



l_forearm_joint:
  control_mode: *ctrl_mode_upperb
  pid:
    position: *pos_gain_arm_sm
    impedance: *default_imp_gain



l_wrist_pitch_joint:
  control_mode: *ctrl_mode_upperb
  pid:
    position: *pos_gain_arm_sm
    impedance: *default_imp_gain



fr_hip_yaw_joint:
  control_mode: *ctrl_mode_lowerb
  pid:
    position: *pos_gain_leg_big
    impedance: *default_imp_gain



fr_hip_pitch_joint:
  control_mode: *ctrl_mode_lowerb
  pid:
    position: *pos_gain_leg_big
    impedance: *default_imp_gain



fr_knee_joint:
  control_mode: *ctrl_mode_lowerb
  pid:
    position: *pos_gain_leg_big
    impedance: *default_imp_gain



fr_ankle_pitch_joint:
  control_mode: *ctrl_mode_lowerb
  pid:
    position: *pos_gain_leg_mid
    impedance: *default_imp_gain



fr_ankle_yaw_joint:
  control_mode: *ctrl_mode_lowerb
  pid:
    position: *pos_gain_leg_yaw
    impedance: *default_imp_gain



fr_wheel_joint:
  control_mode: 71_motor_vel_ctrl
  pid:
    velocity: *default_wheel
    position: *default_pos_gain
    impedance: *default_imp_gain



fl_hip_yaw_joint:
  control_mode: *ctrl_mode_lowerb
  pid:
    position: *pos_gain_leg_big
    impedance: *default_imp_gain



fl_hip_pitch_joint:
  control_mode: *ctrl_mode_lowerb
  pid:
    position: *pos_gain_leg_big
    impedance: *default_imp_gain



fl_knee_joint:
  control_mode: *ctrl_mode_lowerb
  pid:
    position: *pos_gain_leg_big
    impedance: *default_imp_gain



fl_ankle_pitch_joint:
  control_mode: *ctrl_mode_lowerb
  pid:
    position: *pos_gain_leg_mid
    impedance: *default_imp_gain



fl_ankle_yaw_joint:
  control_mode: *ctrl_mode_lowerb
  pid:
    position: *pos_gain_leg_yaw
    impedance: *default_imp_gain



fl_wheel_joint:
  control_mode: 71_motor_vel_ctrl
  pid:
    velocity: *default_wheel
    position: *default_pos_gain
    impedance: *default_imp_gain



hr_hip_yaw_joint:
  control_mode: *ctrl_mode_lowerb
  pid:
    position: *pos_gain_leg_big
    impedance: *default_imp_gain



hr_hip_pitch_joint:
  control_mode: *ctrl_mode_lowerb
  pid:
    position: *pos_gain_leg_big
    impedance: *default_imp_gain



hr_knee_joint:
  control_mode: *ctrl_mode_lowerb
  pid:
    position: *pos_gain_leg_big
    impedance: *default_imp_gain



hr_ankle_pitch_joint:
  control_mode: *ctrl_mode_lowerb
  pid:
    position: *pos_gain_leg_mid
    impedance: *default_imp_gain



hr_ankle_yaw_joint:
  control_mode: *ctrl_mode_lowerb
  pid:
    position: *pos_gain_leg_yaw
    impedance: *default_imp_gain



hr_wheel_joint:
  control_mode: 71_motor_vel_ctrl
  pid:
    velocity: *default_wheel
    position: *default_pos_gain
    impedance: *default_imp_gain



hl_hip_yaw_joint:
  control_mode: *ctrl_mode_lowerb
  pid:
    position: *pos_gain_leg_big
    impedance: *default_imp_gain



hl_hip_pitch_joint:
  control_mode: *ctrl_mode_lowerb
  pid:
    position: *pos_gain_leg_big
    impedance: *default_imp_gain



hl_knee_joint:
  control_mode: *ctrl_mode_lowerb
  pid:
    position: *pos_gain_leg_big
    impedance: *default_imp_gain



hl_ankle_pitch_joint:
  control_mode: *ctrl_mode_lowerb
  pid:
    position: *pos_gain_leg_mid
    impedance: *default_imp_gain



hl_ankle_yaw_joint:
  control_mode: *ctrl_mode_lowerb
  pid:
    position: *pos_gain_leg_yaw
    impedance: *default_imp_gain


hl_wheel_joint:
  control_mode: 71_motor_vel_ctrl
  pid:
    velocity: *default_wheel
    position: *default_pos_gain
    impedance: *default_imp_gain

right_hand:
  control_mode: 3B_motor_pos_ctrl
  pid: 
    position: [100000.0 , 0.0 , 1.0]

left_hand:
  control_mode: 3B_motor_pos_ctrl
  pid:
    position: [100000.0 , 0.0 , 1.0]     
